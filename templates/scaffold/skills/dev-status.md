# Dev Status

`dev/status.md` is the durable current state of the project.

Update it when:

- a feature/incident/deployment changes project state;
- a blocker is discovered;
- a decision affects future sessions;
- a handoff needs a stable reference.
