# Codex Delegate

Use the GPT-5.6 role profile before choosing a transport: Prime = Sol/xhigh for strategy, Plan/Review = Terra/high, Build = Luna/high, Simple = Luna/medium. Develop means Plan → Build → Review.

Use for routine implementation after diagnosis and plan. Required envelope: CODEX PROFILE, TRACKS, TASK, TASK TYPE, BASE SHA, ALLOWED PATHS, RELEVANT MEMORY, DOD, FORBIDDEN, REPORT BACK.

If a writer finds an ambiguity, return `SPEC GAP` rather than inventing scope. Review-gate before commit: inspect diff, allowed paths, DoD, tests and scrub risk.
