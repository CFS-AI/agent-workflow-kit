# Session Handoff

Write `dev/codex-tasks/handoff-YYYY-MM-DD-<slug>.md` with:

- goal;
- current state;
- files touched;
- decisions and constraints;
- verification already run;
- next steps;
- read-first paths.

No secrets or raw private data.
