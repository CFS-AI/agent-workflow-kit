# Demo workspace

This directory is intentionally tiny. `scripts/smoke-test.sh` copies templates into a temporary workspace and validates structure.

Use it to verify the kit before publishing or after edits.
