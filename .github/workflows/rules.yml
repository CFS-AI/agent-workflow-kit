name: Rules

on: [pull_request]

jobs:
  rules:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: cmp --silent CLAUDE.md AGENTS.md
      - run: ./scripts/scrub-check.sh
